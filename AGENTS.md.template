# AGENTS.md — Project Context

> Copy this file to the root of your project as `AGENTS.md`.
> Fill in the blanks. Keep it under 50 lines. Delete this comment block.

## Stack

- Language: <e.g. Python 3.12>
- Framework: <e.g. FastAPI>
- Package manager: <e.g. uv / pip>

## Commands

```bash
# Run tests (Implementer and Reviewer use this)
<e.g. pytest workspace/tests/>

# Lint
<e.g. ruff check src/>

# Type check (optional)
<e.g. mypy src/>
```

## Conventions

- <e.g. snake_case for functions, PascalCase for classes>
- <e.g. tests must not hit the network — use fixtures>

## Non-obvious patterns

- <e.g. all DB queries go through src/db/session.py, never import engine directly>
